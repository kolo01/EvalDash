{% extends 'template.html.twig' %}

{% block title %}Commande{% endblock %}

{% block body %}
    <h1>Commande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande.id }}</td>
            </tr>
            <tr>
                <th>DateCom</th>
                <td>{{ commande.dateCom ? commande.dateCom|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Total</th>
                <td>{{ commande.total }}</td>
            </tr>
            <tr>
                <th>Observation</th>
                <td>{{ commande.observation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commande') }}">back to list</a>

    {# <a href="{{ path('commande_edit', {'id': commande.id}) }}">edit</a> #}

    {# {{ include('cc/_delete_form.html.twig') }} #}
{% endblock %}
